# Re:Bin PWA v2.0.0 — 백서 v2.0 반영 + 버그 수정 🚀

기존 v1.3.4 앱을 **백서(Whitepaper) v2.0** 컨셉에 맞춰 업그레이드했습니다.
"봉투 너머의 시민 인프라" — 측정에서 끝나지 않고 자산화·환원·참여로 이어지는
4단계 자기강화 사이클을 앱 전반에 반영했습니다.

## 🔴 버그 수정

### Bug #1 (CRITICAL): 포인트 화면 렌더링 중단
- `renderPoints()`가 존재하지 않는 DOM 요소 `pts-superbin`에 접근
- `$('pts-superbin').textContent = ...` → `null` 참조 → TypeError
- 결과: 포인트 화면에서 **배지·챌린지 카드가 렌더링되지 않음**
- 수정: 사용하지 않는 `pts-superbin` 라인 제거 (동 단위 순위는 v1.3.0에서 폐지됨)

### Bug #2 (UX): 삭제 안내 문구가 사실과 불일치
- 기록 삭제 확인창: "적립된 포인트도 환수됩니다" 표시
- 실제 `deleteEmission()`은 v1.2.4 이후 포인트를 환수하지 않음 (`pointsRefunded: 0`)
- 토스트도 항상 "-0P"로 표시
- 수정: "이미 적립된 배지·챌린지 포인트는 유지됩니다"로 정정, 토스트 단순화

### Bug #3 (UX): 등급 시스템 이원화 정리
- 기존: 누적 포인트 등급(SEEDLING~DIAMOND) + 감축 등급, 두 개의 등급 카드 공존
- 누적 포인트 등급은 도달 불가능한 임계값(TREE 50만P 등)으로 사실상 의미 없음
- 수정: 감축 등급 하나로 일원화, 등급명은 SEEDLING~DIAMOND 유지(환경 이미지에 적합),
  중복되던 누적 포인트 "활동 레벨" 카드 제거

## ✨ 백서 v2.0 신규 반영

### 1. 감축 성과 5등급 차등 환원 (핵심)
- 백서의 핵심 메커니즘 — 베이스라인 대비 월 감축률로 5등급 산정
- **SEEDLING / SPROUT / TREE / FOREST / DIAMOND** (감축률 0 / 1~5 / 5~15 / 15~30 / 30%+)
- `RebinAPI.getReductionGrade()` 신규 — 로컬 IndexedDB 데이터로 실제 계산
  - 베이스라인 = 설치 후 첫 3개월(현재월 제외) 평균 배출량
  - 진행 중인 달은 배출 추세를 월말 기준으로 투영(예상 등급)
  - 등급 구간 내 선형 보간으로 예상 월 환원 포인트 산정
- 홈 화면 상단에 **"이번 달 감축 등급"** 카드 신규
- 포인트 화면에 **5등급 차등 환원 표** + 현재 예상 등급 표시

### 2. 4단계 자기강화 사이클
- 가이드 화면에 측정 → 자산화 → 환원 → 참여 사이클 시각화

### 3. 단지 공동 환원
- 포인트 화면에 단지 단위 공동 환원(연 100~300만원) 안내 카드

### 4. PCR 85~90% 재활용 카트리지
- 가이드 화면에 PCR 카트리지 친환경 메시지 카드

### 5. v2.0 브랜드 메시지
- "IoT 기반 폐기물 데이터·자원순환 인프라 — 봉투 너머의 시민 인프라"
- 온보딩·메타 태그·매니페스트·앱 정보 문구 일괄 갱신

## 📁 변경 파일

| 파일 | 변경 |
|---|---|
| `data-layer.js` | `getReductionGrade()`·`REDUCTION_GRADES`(SEEDLING~DIAMOND) 신규, APP_VERSION 2.0.0 |
| `app.js` | `renderReductionGrade()` 신규, 누적 포인트 등급 카드 제거, `pts-superbin` 버그 수정, 삭제 문구 정정 |
| `index.html` | 감축 등급 카드, 5등급 표, 단지 공동 환원, 4단계 사이클, PCR 카드, v2.0 메시지, 활동 레벨 카드 제거 |
| `styles.css` | 감축 등급 카드 / 차등 환원 표 / 공동 환원 / PCR 카드 스타일 |
| `service-worker.js` | CACHE_VERSION 2.0.0 |
| `manifest.json` / `README.md` | v2.0 설명 갱신 |

## ⚠️ 파일럿 단계 안내
감축 등급·예상 포인트는 백서 로드맵상 Phase 2부터 실제 환원이 시작됩니다.
파일럿 단계에서는 "예상" 값으로 표시되며 자동 적립은 하지 않습니다.
